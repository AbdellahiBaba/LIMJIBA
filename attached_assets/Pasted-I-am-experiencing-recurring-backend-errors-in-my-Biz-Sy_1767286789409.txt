I am experiencing recurring backend errors in my Biz‑Sync application, including:

- 500 Internal Server Errors during product creation
- DNS resolution failures such as: getaddrinfo EAI_AGAIN helium
- Database schema mismatches between development and production
- Missing or invalid fields causing crashes instead of proper validation errors
- Unhandled exceptions that return generic 500 responses
- Frontend and backend expecting different field names or data types
- Intermittent failures when connecting to external services or the database
- Any other hidden issues that may cause instability

I need you to perform a full diagnostic and repair of the entire system. Specifically:

1. **Inspect all backend routes** (product creation, stock, invoices, authentication, etc.)  
   - Identify any incorrect paths, HTTP methods, or missing middleware.
   - Ensure JSON parsing, validation, and error handling are correct.

2. **Check all database models and schema definitions**  
   - Ensure production and development schemas match.
   - Fix missing fields, wrong types, or constraints.
   - Verify that all required fields exist and are validated.

3. **Fix all DNS, networking, and environment variable issues**  
   - Identify why the backend is trying to resolve the hostname "helium".
   - Correct any invalid DB_HOST, API_URL, or service URLs.
   - Ensure the app uses stable, correct hostnames.

4. **Improve error handling across the entire backend**  
   - Replace generic 500 errors with meaningful messages.
   - Add try/catch blocks where needed.
   - Return proper status codes (400, 404, 409, 422, etc.).

5. **Validate all incoming request bodies**  
   - Add schema validation (Zod, Joi, or custom).
   - Prevent invalid or missing fields from reaching the database.

6. **Ensure frontend and backend are fully aligned**  
   - Field names
   - Data types
   - Required fields
   - API endpoints

7. **Refactor any fragile or duplicated logic**  
   - Make the system more robust and maintainable.

8. **After fixing everything, show me:**  
   - The corrected backend code  
   - Any updated models or schema  
   - Any updated frontend code  
   - A clear explanation of what was wrong and how you fixed it  
   - A list of improvements made to prevent future issues  

Treat this as a full reliability audit and repair. Do not provide partial fixes — I want the entire system stabilized and future‑proofed.
