You are an expert full‑stack engineer and accounting‑aware system architect.
Your task is to fix the Profit Calculator page so it correctly displays the
current financial data (revenue, COGS, salaries, expenses, profit, margin).

Here are the symptoms:
- The profit page shows “Aucune donnée disponible” and 0 values.
- The backend accounting logic is correct and updated.
- The frontend is not reflecting the current data.
- The issue likely comes from outdated API logic, mismatched field names,
  incorrect date filtering, or stale frontend mapping.

Your responsibilities:

1. Inspect the profit API route used by the `/profit` page.
   - Ensure it uses the new historical COGS fields stored on sale/invoice items.
   - Ensure it aggregates POS + B2B revenue correctly.
   - Ensure it includes salaries and expenses.
   - Ensure it uses the correct date field and correct date range logic.
   - Ensure it returns all required fields in a clean JSON structure.

2. Inspect the frontend profit page.
   - Ensure it calls the correct API endpoint.
   - Ensure it maps the returned JSON fields correctly.
   - Ensure it handles empty arrays gracefully.
   - Ensure it displays the correct totals for:
       • Revenue
       • COGS
       • Gross Profit
       • Salaries
       • Expenses
       • Net Profit
       • Profit Margin

3. Fix any mismatched field names between backend and frontend.
   Examples:
   - costPrice → historicalCost
   - total → totalAmount
   - createdAt → date
   - invoiceItems → items

4. Fix date filtering.
   - Ensure the profit page correctly filters by:
       • This month
       • This year
       • Custom date range
   - Ensure timezone issues do not cause empty results.

5. Fix monetary rounding.
   - Ensure all values are rounded to 2 decimals consistently.

6. After fixing:
   - Rewrite the API route cleanly.
   - Rewrite the frontend mapping cleanly.
   - Add comments explaining the corrected logic.
   - Ensure the profit page displays real data again.

