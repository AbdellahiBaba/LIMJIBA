Enhance the existing system with full multilingual support (Arabic + French), professional branding, brand‑color integration, and logo/watermark features. Implement the following:

1. Multilingual Support (Arabic + French)
UI Language System
Add a language switcher (Arabic / French / Bilingual).

Store selected language in local settings.

Move all UI text into translation files:

/locales/ar.json

/locales/fr.json

Ensure all modules (Invoices, POS, Stock, Rewards, Settings) use these translation files.

Arabic RTL Support
When Arabic is selected:

Apply dir="rtl" and lang="ar" to the layout.

Right-align text, tables, and forms.

Use Arabic-friendly fonts (Cairo, Amiri, Noto Kufi Arabic).

Ensure the PDF generator supports RTL text.

Invoice Language Options
Add invoice language setting:

Arabic

French

Bilingual (AR + FR)

Support Arabic and French number‑to‑words conversion.

Adjust invoice layout automatically based on selected language.

2. Logo Upload & Branding System
Branding Settings Page
Create a new page called Branding Settings with:

Upload company logo (PNG/JPG/SVG)

Upload watermark image

Toggle watermark ON/OFF

Choose logo position on invoice (left / center / right)

Choose default invoice language (AR / FR / AR+FR)

Choose brand primary and accent colors

Preview invoice with selected branding

Logo Integration
Display uploaded logo in:

Invoice header

POS receipts (optional)

Dashboard sidebar

Login screen

Watermark System
Use uploaded watermark OR auto-generate one from the logo.

Center watermark behind invoice content.

Opacity: 10–15%

Ensure watermark works in both RTL and LTR modes.

3. Brand Color System
Brand Color Extraction
When a logo is uploaded, automatically extract dominant colors.

Suggest a primary and accent color based on the logo.

Brand Color Application
Apply brand colors to:

Invoice borders and table headers

Buttons and UI components

Sidebar and navigation

POS interface

Headings and highlights

Manual Override
Allow user to manually set:

Primary color

Accent color

Background color (optional)

4. Branded Invoice Styling
Use brand colors consistently in invoice PDF.

Improve spacing, alignment, and typography.

Add branded footer with company contact info.

Add “Preview Invoice” before generating PDF.

Ensure invoice layout adapts to:

Logo position

Language direction (LTR/RTL)

Watermark settings

5. Requirements
All features must work fully offline.

Integrate seamlessly with the existing system.

Ask me only if a critical detail is missing.

Start implementing these enhancements step by step.